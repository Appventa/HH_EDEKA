---
name: project-build-progress
description: "Current build status of the Handelshof ONE / Edeka ONE dual-brand Expo app, per CONTEXT.md build order"
metadata: 
  node_type: memory
  type: project
  originSessionId: 7eb16b59-f88b-4bb4-ba68-449e506e9150
---

Build order steps 1-9 (Splash/Brand Picker/Login/Tab shell, Start dashboard, Kundenkarte, Märkte, Angebote, Scanner, Einkaufsliste, Rechnungen, Profil) are complete and verified (`npx tsc --noEmit` + `npx expo lint` clean).

Next up: no fixed "next step" — app is in a polish/iteration phase. See the entries below for the most recent round of refinements (Start dashboard offer cards, two-row personalized offers, header BrandMark logos + greeting, spacing). The one still-open item is the active-filter-chip contrast issue noted further down.

Rechnungen (step 8) implementation notes: `Invoice`/`InvoiceItem` types extended with `invoiceNumber` and `vatRate` (mock JSON updated for both brands, modeled after real Handelshof eRechnung PDFs the user provided). Added `expo-print` + `expo-sharing` deps. `src/utils/invoicePdf.ts` builds a brand-themed HTML invoice and shares it as a real PDF via `shareInvoicePdf()`. Screen has branch filter chips + date sort toggle + expandable invoice cards with itemized breakdown and "PDF herunterladen" button.

Also added a set of theme-aware decorative SVG/Reanimated illustrations (colored via `brand.colors.accent`): `ScanExplainerIllustration` (Scanner first-run overlay, dismissible via `useOnboardingStore`), `KnifeCutIllustration` (Angebote loading), `PinDropIllustration` (Märkte loading), `BasketIllustration` (Liste empty state). User said these can be refined/restyled later — current versions are functional placeholders.

**Why:** CONTEXT.md's "Build order" checklist is the canonical progress tracker — it's updated with `~~strikethrough~~ (done)` as each screen ships, so any session can see at a glance what's left.

**How to apply:** When picking up this project, read `CONTEXT.md`'s build order section first to know where to continue. After finishing a screen, mark it done there (and update this memory if the next-step pointer changes).

Visual polish pass (post step-8, before step 9) — went through several iterations, settled state:
- Tried gradient screen backgrounds (pastel, then bold per Photoshop refs) — user ultimately rejected gradients entirely.
- Tried frosted-glass (BlurView) absolute/transparent bottom tab bar — user rejected, "needs to be solid color, no transparency". Tab bar reverted to solid `brand.colors.background` with `borderTopColor: brand.colors.border`, normal (non-absolute) layout.
- **Final**: solid CI-color screen backgrounds — `<ScreenBackground />` (`src/components/ScreenBackground.tsx`) renders a plain `View` filled with `brand.colors.primary` (Handelshof red `#E2001A`, Edeka yellow `#FFD500`), absoluteFill behind each screen's content. Replaced the old `ScreenGradient` everywhere (8 files). Removed `gradient`/`gradientLocations` from `BrandColors` entirely, reverted `themed-text`/`themed-view` `color` prop back to plain `keyof BrandColors`.
- `expo-blur` and `expo-linear-gradient` deps left installed but unused — fine to leave, low cost.

**Why:** user wants strong, simple CI-color brand identity, not gradients/blur effects — don't re-suggest gradient or frosted-glass treatments without explicit re-request.

**Resolved:** user confirmed solid CI backgrounds (sent Handelshof website screenshot showing white text on red CI), and asked for white text on red (Handelshof) / blue text on yellow (Edeka) for anything sitting directly on the screen background — `BrandColors.textOnPrimary` already had exactly these values (HH `#FFFFFF`, Edeka `#003B7C`), so no new tokens needed. Applied `color="textOnPrimary"` to all page-level titles/subtitles/hints/icons across index, karte, maerkte, liste, rechnungen, profil. Also fixed a related contrast bug: `KnifeCutIllustration`/`BasketIllustration` and a few icons used `brand.colors.accent`, but for Handelshof `accent === primary` (both red), so they were invisible on the new red bg — switched those to `textOnPrimary` too.

**Resolved:** Profil screen contrast fixed — avatar circle now `backgroundColor: surface` with `accent`-colored icon (was `primary`/`textOnPrimary`, blended into new bg); the account-info section and both action rows (switch brand, logout) now have `backgroundColor: surface` cards (were transparent with just a border, which disappeared on the colored bg) with their original default/textSecondary/error text colors restored (those colors only make sense on light surfaces).

**Still open (deferred to step 9):** the active-state filter chips in Rechnungen/Angebote use `colors.accent` as fill, which for Handelshof equals the bg color (`accent === primary === #E2001A`) — active chips may blend into the background. Same fix pattern likely applies (give them a `surface`-based look or distinct border) when Profil/refinement step is tackled.

**Resolved:** "Aktuelle Angebote" cards on Start dashboard redesigned — added optional `discountPercent?: number` to `Offer` type. Cards changed from square (`width: 160, minHeight: 90`) to rectangular (`width: 220, minHeight: 64`). Price/unit now one line via nested `ThemedText` (bold price + accent color, normal-weight unit with "pro " replaced by "/ "). Discount badge (`-XX%`) shown top-right via `position: absolute`.
- All 8 mock offers per brand (handelshof + edeka-foodservice `offers.json`) now have a `discountPercent` (varied 8-35%) — user pointed out every "Angebot" is by definition a special offer, so all cards should show a badge, not just some.
- Badge styling: `backgroundColor: brand.colors.success` (`#1E8E3E`, same green for both brands) with hardcoded `color: '#FFFFFF'` text — user wanted "GREEN like 'go' time" instead of the initial `accent`/`textOnAccent` combo (which rendered red for Handelshof).

**Resolved:** Start dashboard now has two offer rows. Renamed `strings.start.offersTeaser` → `offersTeaserGeneral` ("Allgemeine Angebote") and added `offersTeaserIndividual` ("Individuelle Angebote"). Second row shows offers filtered by `PERSONALIZED_CATEGORIES = ['Fleisch', 'Fisch']` (a hardcoded const in `index.tsx`), simulating "recommended based on past purchases" — there's no real purchase-history data model yet, this is fully faked. Extracted `OfferCard`/`OfferCardSkeleton` as local components in `index.tsx` to share markup between both rows.

**Why:** user wants to lay groundwork for future real personalization (purchase-history-driven recommendations) without building the actual history tracking yet — keep this in mind if/when a real "Kaufhistorie" data source is added later (would replace the hardcoded category filter).

**Resolved:** Added `BrandMark` (`src/components/BrandMark.tsx`) — SVG recreation of each brand's logo mark (Handelshof: white square/red border/red "H"; EDEKA: blue square with yellow inset + blue "E"), based on a reference image of both real logos. Placed in the tab navigator's `headerLeft` (top-left, all tabs) via `src/app/(tabs)/_layout.tsx`. Note: a separate pre-existing `BrandLogo.tsx` (text-badge placeholder, used on login/brand-picker) is unrelated and untouched — don't conflate the two.

**Resolved:** Header greeting changed from "Guten Tag, {businessName}" to "Willkommen, {userName}" to save space — `strings.start.greeting` updated accordingly. Added `userName: string | null` to `useAuthStore` (set to `'Herr Shah'` on login, cleared on logout); `_layout.tsx` now reads `userName` instead of `businessName` for the header title.

**Resolved:** Tightened vertical spacing on Start dashboard to reduce scrolling — `index.tsx` `content` gap 16→8px and new `offersSectionTitle` style (marginTop 4px) applied to both "Angebote" section titles. Card sizing (`offerCard`/`offerCardSkeleton` padding/gap/minHeight) was initially shrunk too but reverted — user only wanted spacing *between* rows reduced, not the cards themselves. Don't resize cards again unless explicitly asked.

**Resolved:** Brand-picker (Welcome) screen — increased gap between the two brand cards (`Spacing.four`→`Spacing.five`) and added `paddingBottom: Spacing.six` to the `cards` container (still `justifyContent: 'center'`) to shift both cards slightly upward. File: `src/app/brand-picker.tsx`.

**Resolved:** Real brand assets integrated (user provided 3 files via `tmp assets images/`, now moved into `assets/`):
- `assets/images/splash-brands.webp` — splash screen now shows this full-bleed image (`src/app/index.tsx`), replacing the old animated "ONE" text. Kept a simple fade-in + the same `SPLASH_DURATION`/routing logic.
- `assets/brands/handelshof/logo.png` and `assets/brands/edeka-foodservice/logo.png` — real app-icon-style logos (each already includes the "Handelshof ONE"/"EDEKA ONE" wordmark). `src/components/BrandLogo.tsx` rewritten to render these via `Image` + `require()` (static `LOGOS` map keyed by `brand.id`) instead of the old text-badge placeholder. Used on brand-picker (large, 120px) and login header (small, 56px).
- Brand-picker (`src/app/brand-picker.tsx`): removed the separate `cardLabel` text under each logo (redundant now that the logo image contains the brand name).
- `BrandConfig.logo.{light,dark}` fields remain unused placeholder strings — not wired up, no other code references them.

**Resolved:** Login screen header (`src/app/login.tsx`) — logo bumped from `size="small"` (56px) to `size="large"` (120px) and the whole header (logo, title, subtitle) is now center-aligned (`alignItems: 'center'` + `textAlign: 'center'` on title/subtitle) — was previously left-aligned/too small.

**Resolved:** New "Rückvergütung" (cashback) feature added. Start dashboard `TILES` (`src/app/(tabs)/index.tsx`) reordered/changed: `karte`, `scanner` (moved here from bottom tab, top-right), `rechnungen`, `rueckverguetung` (new, bottom-right) — the old `maerkte` tile was removed entirely from quick-access (Märkte stays as a bottom tab; `strings.start.tileMaerkte` removed as it became unused).
- New screen `src/app/rueckverguetung.tsx`, registered in `src/app/_layout.tsx` Stack (`headerShown: true, title: 'Rückvergütung'`).
- New data: `Loyalty { totalSpend: number }` in `src/data/types.ts`, mock JSON per brand (`src/data/mock/{brand}/loyalty.json`: Handelshof 6180.45€, Edeka 2840.00€ — chosen so each brand demos a different reached tier + progress toward the next), `src/data/services/loyalty.ts` (`getLoyalty(brandId)`, same `delay()` pattern as `customerCard.ts`).
- New `src/utils/loyalty.ts`: `LOYALTY_TIERS` (2500€→10%, 5000€→12%, 10000€→15%, cumulative — highest threshold reached sets the flat rate applied to total spend) and `getLoyaltyStatus(totalSpend)` returning `{ currentRate, cashback, nextTier, progress }`.
- Screen shows two side-by-side cards (Gesamtumsatz / Rückvergütung bisher + current rate note), a progress bar + "Noch X € bis Y% Rückvergütung" (or "höchste Stufe erreicht" if maxed), and a tier list with checkmarks for reached tiers. New strings under `strings.rueckverguetung` + `strings.start.tileRueckverguetung`.
- `npx tsc --noEmit && npx expo lint` clean after this change.

Known shared color/token convention established during this build: `BrandColors` has both `textOnPrimary` (for text on `colors.primary`-colored backgrounds, e.g. badges/avatars) and `textOnAccent` (for text on `colors.accent`-colored backgrounds, e.g. active chips, add-to-list buttons, nearest-branch card). Don't conflate the two — EDEKA's `accent` and `primary` use different colors than `textOnPrimary`/`textOnAccent` would suggest if reused incorrectly.

**Resolved (2026-06-13):** Added three new B2B workflow features unifying "Verfügbarkeit checken" (off-site availability check), "Scan & Go" (in-store scan-to-cart), and "Lieferung anfragen" (delivery request), built on a shared `availableBranchIds: string[]` field added to `Product` (and mirrored onto `ShoppingListItem`/`AddableItem`/`CartItem`). Key pieces:
- Scanner screen now has a `ScannerModeToggle` (`src/components/ScannerModeToggle.tsx`) — "Zuhause" (planning, adds to shopping list + shows `AvailabilityBadge`) vs. "Im Markt" (instore, adds to new `useCartStore` Warenkorb + branch-chip picker, floating cart bar → `/kasse`).
- New `src/state/useCartStore.ts` (separate from `useShoppingListStore`) holds the Scan & Go cart, persisted per-brand.
- New `src/components/AvailabilityBadge.tsx` renders "Verfügbar in allen/X von Y Filialen / Aktuell nicht verfügbar" given `availableBranchIds` + total branch count; used in Scanner result card and each Liste row.
- Liste screen footer got two new buttons: "Filialbesuch planen" → `/filialbesuch` (ranks branches by how many list items are in stock there, shows best-match badge + missing items per branch) and "Lieferung anfragen" → `/lieferung` (order summary + `CustomerCard.address` + branch picker, submits and clears the list).
- New `/kasse` screen shows a Code128 `Barcode` transaction code + cart summary + "Als bezahlt markieren" (clears cart).
- All three new screens registered in `src/app/_layout.tsx` as `headerShown: true` stack screens. `CustomerCard` type gained an `address` field (mock data updated for both brands).
- `npx tsc --noEmit && npx expo lint` both clean after this change.

**Why:** user wants this as groundwork for realistic B2B restaurant-supply workflows (check stock before driving to a branch, scan-and-pay while in-store, or have a list delivered) — all three share the same `availableBranchIds`/shopping-list data model, so don't build a parallel data path if extending these flows further.

**Resolved (2026-06-13):** Added GPS geofence check gating "Im Markt" (Scan & Go) mode. `src/utils/geo.ts` gained `STORE_GEOFENCE_RADIUS_KM = 0.5` and `findBranchAtLocation(lat, lng, branches)` (nearest branch within radius, or undefined). In `scanner.tsx`, `handleModeChange` (passed to `ScannerModeToggle.onModeChange`) requests `expo-location` foreground permission + current position on switch to "instore"; if a branch is within 500m it auto-selects that branch (`locationVerified = true`, shows a non-interactive "📍 Sie sind hier: X" pill instead of the branch-chip picker). If no branch is in range or permission is denied, shows `Alert.alert` with brand-appropriate title/hint (`notAtStoreTitle`/`locationDeniedTitle`) and a "Trotzdem fortfahren" override that falls back to manual branch-chip selection (`locationVerified = false`) — this override exists deliberately so the feature stays testable/usable when GPS is weak indoors or the tester isn't physically near the mock branches (real German cities). `ScannerModeToggle` gained `checking`/`locationVerified` props and an `ActivityIndicator` loading state on the "Im Markt" segment during the check. `app.json` location permission description text updated to mention Scan & Go.

**Why:** user explicitly asked for a "smart" check to ensure Scan & Go is only usable when physically at a store — full enforcement without an override would make the feature untestable for anyone not near the mock branches' real coordinates, so a soft-gate-with-override was chosen as the pragmatic default. If the user wants a hard block (no override), that's a one-line change: remove the second Alert button.

**Resolved (2026-06-13):** Reworked the geofencing UX after user feedback that the chip-picker/alert flow felt "weird":
- `ScannerModeToggle` simplified to just the "Zuhause"/"Im Markt" segmented control (no branch picker, no "Sie sind hier" pill inside it).
- `src/utils/geo.ts`: `findBranchAtLocation`/`STORE_GEOFENCE_RADIUS_KM` replaced by `findNearestBranch()` (always returns closest branch, no radius) for the manual toggle, and a new `findBranchNearby()` + `STORE_PROXIMITY_RADIUS_KM = 0.5` (returns nearest branch only if within 500m) for the login-time auto-check.
- `scanner.tsx`: manual "Im Markt" tap now silently resolves to `findNearestBranch()` (or `branches[0]` fallback) — no Alert/override anymore. A new full-width **location bar** renders directly under the native header (in `(tabs)/_layout.tsx` header, not the camera overlay) whenever `mode === 'instore'`: shows a spinner + "Standort wird geprüft…" while resolving, then "📍 Sie sind hier: {Filiale}".
- **Login-time auto-detection** (this request): `src/app/brand-loading.tsx` (the existing 1.4s animated transition between brand-picker and login) now also runs a one-time `expo-location` check via `findBranchNearby()`. If the user is within 500m of a branch, `useCartStore.mode[brandId]` is set to `'instore'` + `activeBranchId` to that branch (location bar shows immediately, no flash, when Scanner opens); otherwise `mode` is set to `'planning'`. This OVERWRITES the persisted `mode` every session/login — intentional, so the start state reflects current reality rather than stale last-session state. The manual toggle in Scanner remains as an override/demo path (uses the no-radius `findNearestBranch` fallback).
- Removed unused strings `notAtStoreTitle/Hint`, `locationDeniedTitle/Hint`, `continueAnyway`.
- Reordered both `branches.json` files so the Köln/Essen-region branch is first (Handelshof: Essen; EDEKA: Köln) — used as the `branches[0]` fallback for the manual toggle and shown first in branch lists, since the app will be pitched to a Cologne client.
- `liste.tsx`: footer's first action button is now mode-aware — `strings.liste.checkoutInStore` ("Checkout in der Filiale" → `/scanner`) when `mode === 'instore'`, else `strings.liste.planVisit` ("Filialbesuch planen" → `/filialbesuch`) when away. "Lieferung anfragen" unchanged in both.
- `npx tsc --noEmit && npx expo lint` clean after all of the above.

**Why:** ties the in-store vs. off-site UX together end-to-end — login-time GPS check picks the right workflow by default for both Scanner (Zuhause/Im Markt) and Liste (plan visit vs. checkout in store), the manual toggle stays only as a demo override for when real GPS doesn't match (e.g. testing away from the prioritized Köln/Essen branches).

**Resolved (2026-06-13):** Brand-switcher relocated + new "Team-Autorisierungen" feature added:
- `src/app/(tabs)/_layout.tsx`: `headerLeft`'s `<BrandMark />` is now wrapped in a `Pressable` (`handleSwitchBrand` = `clearBrand()` + `router.replace('/brand-picker')`), replacing the old dedicated "Marke wechseln" button.
- `src/app/profil.tsx`: removed the "Marke wechseln" button/handler (kept `clearBrand`/`useBrandStore` import for `handleDemoReset`); added a new "Team-Mitglieder autorisieren" button (`strings.profil.team`, icon `people-outline`) → `router.push('/team')`. `strings.profil.switchBrand` was left in place since `login.tsx` still uses it.
- New brand-scoped persisted store `src/state/useTeamStore.ts` (`one-app-team`): `TeamAuthorization { id, name, email, role, startDate, endDate }` (ISO `YYYY-MM-DD`), `addAuthorization`/`removeAuthorization`, hard cap `MAX_TEAM_AUTHORIZATIONS = 10` (silently no-ops past the cap).
- New screen `src/app/team.tsx`, registered in `src/app/_layout.tsx` (`headerShown: true, title: 'Team-Autorisierungen'`). Toggleable inline form (name/email/role-chip-picker from `strings.team.roles` array/date-duration via two steppers — "Beginnt" 0-14 days default 0, "Dauer" 1-30 days default 3, no date-picker lib used) → on submit calls `addAuthorization` + simulated `Alert.alert(strings.team.invitedTitle, strings.team.invitedHint(email))`. Below: list of `TeamMemberCard`s (name/email/role badge/date range/status badge Aktiv·Geplant·Abgelaufen derived from ISO-string comparison vs. today/"Entziehen" revoke button), or an empty state if none yet. New `strings.team` section added.
- `npx tsc --noEmit && npx expo lint` clean after this change.

**Why:** simulates the "Metro-style" co-worker card-authorization flow the user described (owner authorizes a colleague by name/email/date-range, who'd click an email link to activate) — explicitly a prototype/demo, no real auth backend. If a real activation flow is ever requested, `useTeamStore` + `team.tsx` is the place to extend.

**Resolved (2026-06-13):** Replaced GPS-based "Zuhause"/"Im Markt" detection with a manual location-simulation popup, and made the "Sie sind hier" location bar global:
- Deleted `src/components/ScannerModeToggle.tsx` entirely (and its now-unused `strings.scanner.modePlanning/modeInStore/checkingLocation`). `scanner.tsx` no longer imports `expo-location` or `findNearestBranch` — it's now "clean": just camera + scan result + cart bar, reading `mode`/`activeBranchId` from `useCartStore` read-only.
- `src/app/brand-loading.tsx`: removed the one-time `expo-location` + `findBranchNearby` pre-detection effect (and its `useBrandStore`/`useCartStore`/`getBranches` imports) — only the splash fill/scale animation + `setTimeout` → `/login` remains.
- New `src/app/standort.tsx` — full-screen modal (`presentation: 'modal'` in `_layout.tsx`) shown right after login (`login.tsx`'s `handleSubmit` now does `router.replace('/standort')` instead of `/(tabs)`). Explains (new `strings.standort` section) that this simulates a GPS check for the prototype, then lists "Nicht in der Filiale" (away) + one row per branch (`getBranches`); tapping a row calls `setMode`/`setActiveBranch` on `useCartStore` and `router.replace('/(tabs)')`.
- New `src/components/LocationBar.tsx` — plain `View` (no SafeAreaView/insets, `width: '100%'`), shows "📍 Sie sind hier: {Filiale}" when `mode === 'instore'`. Added as the first child (after `<ScreenBackground />`, or first in `styles.overlay` for scanner) on all 6 tab screens: `index`, `karte`, `scanner`, `angebote`, `maerkte`, `liste` (both empty/full branches).
- Left `findNearestBranch`/`findBranchNearby`/`STORE_PROXIMITY_RADIUS_KM` in `src/utils/geo.ts` as now-unused exports (harmless, not flagged by tsc/lint) — fine to remove in a future cleanup pass if asked.
- `npx tsc --noEmit && npx expo lint` clean after this change.

**Why:** user found the in-scanner mode toggle + automatic GPS check confusing for testing ("wrongly... in the scanner screen") — a single explicit choice right after login, applied app-wide via the global `LocationBar` + existing `useCartStore.mode`, is simpler to demo and test different states. `LocationBar` is deliberately a plain `View` (not `SafeAreaView`) to avoid double-counting `insets.top`, since React Navigation's tab header already handles the top inset on every tab screen.

**Resolved (2026-06-14):** `<LocationBar />` is now consistently positioned "flush below the header" on EVERY page/function (not just the original 6 tabs), and `/standort` was simplified to 2 options:
- `scanner.tsx`: moved `<LocationBar />` out of the `edges={['top','left','right']}` title-overlay `SafeAreaView` to be a direct sibling before it (no `'top'` edge) — this was the root cause of the bar appearing in a different position than other tabs.
- `profil.tsx`: was missing an `edges` prop entirely (defaulted to include `'top'`, causing extra padding) — fixed to `edges={['left','right','bottom']}` and added `<LocationBar />`.
- Added `<LocationBar />` (+ import) as the first child after `<ScreenBackground />` (inside a `SafeAreaView` without `'top'` in `edges`) to all remaining pushed screens: `rechnungen.tsx`, `produkt.tsx`, `team.tsx`, `rueckverguetung.tsx`, and all return-branches of `lieferung.tsx`, `kasse.tsx`, `filialbesuch.tsx`.
- Established pattern for any FUTURE screen: `<LocationBar />` must be the first child right after `<ScreenBackground />`, and the enclosing `SafeAreaView`'s `edges` array must NOT include `'top'` (the native header already accounts for `insets.top`).
- `src/app/standort.tsx`: simplified from "away + one row per branch" down to exactly 2 options — "Nicht in der Filiale" (unchanged) and a single "In der Filiale" option using `branches[0]` (Essen for Handelshof / Köln for Edeka Foodservice, per the existing branch-ordering convention noted above). All other per-branch rows removed.
- `npx tsc --noEmit && npx expo lint` clean after this change.

**Why:** user reported the location bar "shows differently in every app section/page/function" and wanted it as "an extension of the topbar downwards" everywhere — consistency required both fixing the two outliers (scanner, profil) and extending coverage to screens that never had it. The standort simplification reuses the prior `branches[0]` reordering so no new filtering logic was needed.

**Resolved (2026-06-14):** Enlarged `ProductImage` sizes across the app and made Start dashboard offer cards interactive:
- Image sizes bumped: `(tabs)/index.tsx` dashboard OfferCard 48→64, `(tabs)/angebote.tsx` OfferCard 56→72 (+`iconWrap` style updated to match), `(tabs)/liste.tsx` ListRow 48→64, `produkt.tsx` header 88→110.
- `(tabs)/index.tsx` `OfferCard` (dashboard "Aktuelle/Individuelle Angebote" rows) was previously a static, non-interactive `View`. Now: the image+title+price row is wrapped in a `Pressable` that navigates to `/produkt` (same params pattern as `angebote.tsx`/`liste.tsx`), and a new full-width "Zur Liste"/"Hinzugefügt" button (reusing `strings.angebote.addToList`/`added`, same green-on-add pattern as `angebote.tsx`'s `addButton`) was added below, wired to `useShoppingListStore.addItem` via a new `handleAdd`/`addedId` state in `StartScreen` (mirrors `angebote.tsx`'s pattern exactly).
- Replaced `useRouter()` hook with the `router` singleton import from `expo-router` in `index.tsx` (needed so the extracted `OfferCard` component can navigate) — tile navigation (`router.push(tile.route)`) works unchanged with the singleton.
- `offerCard` style: removed `justifyContent: 'center'`/`minHeight: 80`, added `gap: Spacing.two` (card now grows naturally with the new add-button row); `offerCardSkeleton` height bumped 80→140 to match.
- `npx tsc --noEmit && npx expo lint` clean after this change.

**Why:** user wanted bigger product imagery throughout and a way to act on dashboard offer cards directly (view full product / quick-add to shopping list) without leaving the Start screen — reused existing `/produkt` navigation and `useShoppingListStore` add-to-list patterns already established in `angebote.tsx` rather than inventing new ones.

**Resolved (2026-06-14):** Fixed two issues reported right after the image-size pass above:
- `(tabs)/liste.tsx` `ListRow` image dialed back from 64→56 (the 64px size made rows look too tall/empty relative to the ~54px text block — 56px is a better match while still bigger than the original 48).
- "Im Markt" (in-store) mode was showing the cross-branch `AvailabilityBadge` ("Verfügbar in X von Y Filialen"), which doesn't make sense once the user is physically at a specific branch. **Final approach** (after a first attempt with a per-branch "Hier verfügbar/nicht verfügbar" variant was reconsidered): `AvailabilityBadge` is now hidden ENTIRELY in `liste.tsx` rows when `inStore` is true — `ListRow` takes a new `inStore: boolean` prop and wraps the badge in `{!inStore && (...)}`. `AvailabilityBadge.tsx` is back to its original signature (`availableBranchIds`, `branchCount`, `colors` only, no `activeBranchId`). This mirrors `scanner.tsx`'s existing `{mode === 'planning' && (<AvailabilityBadge .../>)}` gate — same convention, two call sites.
- `npx tsc --noEmit && npx expo lint` clean after this change.

**Why:** user pushed back on showing ANY availability info on Liste items while in-store: the actual in-store purchase flow is Scanner → `useCartStore` (Scan & Go) — a scanned item is physically in hand, so its availability is self-evident. The Einkaufsliste is the off-site planning list; once in-store, availability badges on it are noise, not signal. Lesson for future in-store-mode features: default to HIDING off-site/planning-oriented UI in-store rather than reframing it for the in-store context — simpler is "senseful" per the user's framing.

**Resolved (2026-06-14):** All product/offer cards across the app standardized to a 3-section vertical layout (title was wrapping up to 3 lines and bloating card height):
- Pattern: **Header** (title `numberOfLines={1}` ellipsis + optional trailing element e.g. discount badge) → **Body row** (`ProductImage`/icon left + extra info, `alignItems: 'center'`) → **Footer row** (`flexDirection: 'row', justifyContent: 'space-between', alignItems: 'center'`: price/unit left, primary action right).
- Applied to: `(tabs)/index.tsx` dashboard `OfferCard`, `(tabs)/produkte.tsx` (then `angebote.tsx`) `OfferCard`, `(tabs)/liste.tsx` `ListRow`, `(tabs)/scanner.tsx` `ProductResult`. Each got renamed/added styles (`*Main`/`*BodyRow`/`*Info`/`*Footer`) following this pattern — skeleton heights adjusted to match new card heights.
- `npx tsc --noEmit && npx expo lint` clean after this change.

**Resolved (2026-06-14):** Dashboard "Angebote" cards reworked into a clean tap-to-preview pattern (user disliked the inline "Zur Liste" button cluttering the card):
- `(tabs)/index.tsx` `OfferCard` is now the whole card as one `Pressable` (signature `{ offer, onPress }`), showing only title (1 line, + discount badge if any) → image → price/unit. No add button on the card itself.
- New `OfferPreviewModal` component (`Modal transparent animationType="fade"`, backdrop `Pressable onPress={onClose}` + inner `Pressable onPress={() => {}}` to swallow taps) shows title + close (X), a tappable body (96px image + description + `validUntil`, navigates to `/produkt` and closes), and a footer with price/unit + the "+"/"Hinzugefügt" add-to-list button (reuses `handleAdd`/`addedId` state, same checkmark-feedback pattern as `produkte.tsx`).
- New styles added: `modalBackdrop`, `modalCard`, `modalBodyRow`, `modalInfo`, `modalFooter`, `modalPriceRow`, `modalAddButton`.
- `npx tsc --noEmit && npx expo lint` clean after this change.

**Resolved (2026-06-14):** Renamed the "Angebote" tab to "Produkte" to distinguish the general product catalog from dashboard-only "Angebote" (discount specials):
- `src/app/(tabs)/angebote.tsx` → `git mv`'d to `src/app/(tabs)/produkte.tsx`; component renamed `AngeboteScreen` → `ProdukteScreen`. Route changed from `/angebote` to `/produkte` — no other code referenced the old route via `router.push`, so this was safe.
- `_layout.tsx`: `Tabs.Screen name="angebote"` → `name="produkte"`, `title: strings.tabs.angebote` → `strings.tabs.produkte` ('Produkte'). Icon (`pricetags-outline`) unchanged per user request.
- `strings.ts`: `tabs.angebote` key renamed to `tabs.produkte` ('Produkte'). The `strings.angebote.*` namespace (addToList/added/validUntil/allCategories/title) was deliberately LEFT AS-IS — these are shared microcopy reused by `produkte.tsx`, dashboard `index.tsx`, and `scanner.tsx`; renaming the namespace would be a cosmetic-only refactor across 3 files for no functional gain. `strings.angebote.title` ('Angebote') remains unused (pre-existing dead string, not introduced by this change).
- `liste.tsx` emptyHint updated: "Fügen Sie Artikel über Angebote oder Produkte hinzu." (was "...über die Angebote...").
- Data model unchanged: `produkte.tsx` still reads `getOffers()`/`Offer[]` (same dataset dashboard "Angebote" pulls from) — the conceptual split is purely presentational: dashboard cards show the `-X%` discount badge (= "Angebote"/specials framing), Produkte-tab cards don't show that badge and instead show description + "Gültig bis" (= general catalog framing). If a deeper data-model split (e.g. separate "regular products without validUntil/discount" dataset) is wanted later, that's a bigger follow-up — not done here.
- `npx tsc --noEmit && npx expo lint` clean after this change.

**Why:** user clarified "Angebote" (-30% etc) should be a dashboard-only featured concept, while the dedicated tab is a general "Produkte" catalog of available items — minimal rename + relabel was the right scope since the visual distinction (discount badge only on dashboard) already existed.

**Resolved (2026-06-14):** New screen `src/app/liste-kasse.tsx` (route `/liste-kasse`, Stack title "Kasse") — when `inStore`, Liste's "Checkout in der Filiale" button now routes here instead of `/scanner` (icon changed `scan-outline` → `card-outline`). Shows the shopping-list items + total (same layout as `/kasse`'s item list), plus a footer with two buttons:
- "Bluetooth aktivieren" — simulated toggle (no real native Bluetooth API/deps used, consistent with other simulated flows like `/standort`'s GPS check); turns `success`-green with a checkmark once pressed, stays active (no revert).
- "Karten-Code anzeigen" — opens a full-screen white modal (same pattern as `karte.tsx`'s `BarcodeZoomModal`) showing the customer's `customerNumber` as a large `Barcode`, for the store employee to scan and validate the list in their system. Reuses `getCustomerCard(brandId)`.
- New `strings.listeKasse` section added (title, subtitle, itemsTitle, total, activateBluetooth/bluetoothActive, showCardCode/cardCodeTitle/cardCodeHint, empty/emptyHint).
- Note: `/kasse` (Scan & Go cart checkout) and `/liste-kasse` (shopping-list checkout) are separate routes that both title "Kasse" — they serve different stores (`useCartStore` vs `useShoppingListStore`) and are reached via different flows, so the title collision is cosmetic only.
- `npx tsc --noEmit && npx expo lint` clean after this change.

**Why:** user wants the in-store "Checkout in der Filiale" flow to end with the staff-facing validation step (scan the customer's card code to pull up/validate their list in the store system) rather than dropping the user back into the Scanner — Bluetooth activation is included as a simulated "connect to store system" gesture, matching the prototype's existing pattern of simulating hardware/location checks rather than wiring real native APIs.

**Resolved (2026-06-14):** Built + verified a release APK covering all three changes above (modal styles, Produkte rename, `/liste-kasse` screen). Build succeeded with the corrected `JAVA_HOME` (see [[project_android_native_build]]). Installed/launched cleanly on `Medium_Phone_API_36.1` (no crashes in logcat); Produkte tab confirmed showing renamed catalog with 3-section cards, Liste footer shows "Checkout in der Filiale" with existing list items. Output handed to user at `android/app/build/outputs/apk/release/app-release.apk` (~119MB) — user said they'll do further testing themselves.

**Resolved (2026-06-17):** Inventory → Products navigation, brand-switcher scoping, layout fixes, Lieferung success screen, and Kasse bug fixes:

- **Inventory data harmonisation**: expanded both brands' `offers.json` and `products.json` to match all 16 inventory items (Handelshof 11→19 offers/13→21 products; EDEKA-Foodservice 11→16 offers/13→17 products). Added `productImageId?: string` field to `InventoryItem` type and all entries in `src/data/mock/shared/inventory.json`. Prices vary realistically between brands.
- **Inventory cards tappable**: `InventoryCard` in `inventar.tsx` is a `Pressable`; tapping navigates to `/(tabs)/produkte` with `params: { q: firstWord }` (first word of item name as search query). A `chevron-forward` icon appears on cards with a linked product. `produkte.tsx` reads the `q` param via `useLocalSearchParams`, pre-fills the search bar, then clears the param.
- **Brand-switcher scope**: `(tabs)/_layout.tsx` global `headerLeft` is now a plain back arrow (`BackButton` component, navigates to `/(tabs)/`). Only the `index` screen overrides `headerLeft` with the full brand-switcher (chevron + logo). All other tabs show the simple arrow.
- **Handelshof logo alignment fixed**: HH logo WebP dimensions are 1037×608 (ratio ~1.71); at height 32 → width = 55px. `headerLogoHH: { width: 55, height: 32 }` eliminates the offset caused by React Native centering an unbound-width Image in a fixed-height container.
- **Tab bar labels restored**: `tabBarShowLabel: true`, `tabBarLabelStyle: { fontSize: 10 }`. Labels: Start, Karte, Inventar, Produkte, Einkaufsliste (hard-coded `tabBarLabel`), Warenkorb (hard-coded `tabBarLabel`).
- **Lieferung success screen redesign**: new `DELIVERY_STAGES` const + `DeliveryStage` component (vertical tracker: green check for done, solid accent for active, hollow ring for upcoming). Success layout: `LocationBar` at top → `ScrollView` with icon + title + delivery date card (tomorrow, 07:00–09:00) + 5-stage tracking card → subtle ghost pill "Fertig" button at bottom (`doneFooter`/`doneButton` with `borderRadius: 20, borderWidth: 1`). Copy: "Deine Lieferung wird vorbereitet" / "Dein Markt hat die Anfrage erhalten…".
- **LocationBar centering bug fixed** (lieferung + liste + kasse): pattern — `SafeAreaView` must be plain `flex: 1` with no `justifyContent: 'center'`; centered content goes in an inner `View` with `flex: 1, alignItems: 'center', justifyContent: 'center'`. Applied in `lieferung.tsx` (empty state), `liste.tsx` (empty state), and `kasse.tsx` (success state). Previously `styles.body` was merged directly onto `SafeAreaView`, dragging `LocationBar` into the centered stack.
- **Kasse success "Fertig" button fix**: `actionButton` has `flex: 1` (needed for the footer row). In the success state (flex column), this stretched the button to fill all remaining vertical space. Fix: dedicated `successButton` style without `flex: 1` (uses `paddingHorizontal: Spacing.five` instead), applied only in the success Pressable.
- **Kasse success state persistence fix**: after paying and tapping "Fertig", returning to the Kasse tab re-showed the success screen (tab screens stay mounted). Fix: `useFocusEffect(useCallback(() => () => setPaid(false), []))` in `kasse.tsx` — resets `paid` on blur so next visit starts fresh at empty cart.

**Why:** user wanted inventory page items cross-linked to the product catalog for quick "add to list" access, a cleaner navigational hierarchy (switcher only on home), and several visual/UX polish fixes across the delivery and checkout flows.
