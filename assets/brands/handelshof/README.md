# Handelshof ONE — brand assets

Drop real assets here, matching the paths referenced in `src/brands/handelshof.ts`:

- `logo-light.png` / `logo-light.svg` — logo for light backgrounds
- `logo-dark.png` / `logo-dark.svg` — logo for dark backgrounds

Once added, update `BrandLogo` (`src/components/BrandLogo.tsx`) to render an `<Image>`
instead of the text placeholder.
